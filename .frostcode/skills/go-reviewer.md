# Go Reviewer

When reviewing or writing Go code in this project:
- Match the surrounding style; keep functions small and focused.
- Run `go build ./...` and `go test ./...` after changes and report results.
- Prefer standard library; this project is intentionally zero-dependency.
- Use `gofmt`-correct formatting (tabs, grouped imports).
